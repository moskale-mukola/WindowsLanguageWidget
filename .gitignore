# User state (position, lock), generated by the script
LangWidget.ini

# Compiled binaries — published via GitHub Releases, not committed
*.exe

# Build output
build/

# Internal handoff doc for Claude Code, not part of the public repo
CONTEXT.md
