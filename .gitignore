# Dependencies
node_modules/

# Rust / Tauri build output
/src-tauri/target/
/src-tauri/gen/schemas/

# Compiled installers/binaries — published via GitHub Releases, not committed
*.exe
*.msi

# User state (generated at runtime in the OS config dir; never in-repo)
settings.json

# Internal handoff doc for Claude Code, not part of the public repo
CONTEXT.md
