{
  "name": "keyboardlanglock",
  "version": "1.0.0",
  "description": "Keyboard layout widget with a change lock",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
